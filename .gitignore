# ============ 一、本地与私有内容：绝不进仓库 ============
# 项目记忆/脚本/临时件（含本地服务日志、探针、Edge 缓存）
.deepworks/

# 后台管理令牌（明文，泄露=任何人都能删反馈）
db/admin_token.txt

# 本地自测数据库（含测试留言）
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm

# Python 缓存
__pycache__/
db/__pycache__/
*.pyc

# ============ 二、课程作业材料：不上线 ============
# 各版本整页截图与源码快照（体积大，且会与线上源码重复）
版本存档/
作业提交区/

# 构建产物/交付物副本（线上直接用根目录源码）
outputs/

# 会话里上传的原始素材（截图、docx、转换中间件）—— 属于对话资料，不是站点文件
uploads/

# ============ 三、系统与编辑器 ============
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
*.log
